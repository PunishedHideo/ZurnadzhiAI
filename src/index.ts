/*
"scripts": {
    "build:dist": "tsc",
    "build:bin": "", // build from dist to bin using "yao-pkg" lib
    "start": "ts-node src/index.ts",
    "watch": "tsc --watch",
*/

// TODO On BYOND use WINAPI - Edge Webview2 API to get tgui_say.html devtools/IndexedDB info
// It is impossible to natively use winapi in js/ts so use node-ffi as a liaison


